# Images

## Logo and favicon

- **RarePepeWorld_logo.png** — Site logo used in the header (height 50px) and as the favicon / browser tab icon on all pages.
- **logo.svg** — Legacy placeholder (green circle + “=8”); kept for reference. The site now uses `RarePepeWorld_logo.png` for both the emoticon and header.
